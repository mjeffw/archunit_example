package org.clean.hexarch.adapter.repo;

import org.clean.hexarch.adapter.addressbook.data.types.RequestVO;
import org.clean.hexarch.application.domain.service.port.RepositoryPort;

public class JdbcRepository implements RepositoryPort {
  RequestVO request;
}
